import React from "react";
import "./Input.css";

const Input = ({ selectedTraversal, handleChange }) => (
    <div className="TraverseType-container">
      <input type="number"></input>
    </div>
  );
  
  export default Input;